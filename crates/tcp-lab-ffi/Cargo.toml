[package]
name = "tcp-lab-ffi"
version = "0.1.0"
edition = "2024"

[lib]
name = "tcp_lab_ffi"
crate-type = ["rlib", "cdylib"]

[dependencies]
tcp-lab-core = { path = "../tcp-lab-core" }
tracing = { workspace = true }


